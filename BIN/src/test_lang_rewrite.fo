extern fn printInt(x: i32);

let x: i32 = 10;
let y: i32 = 5 + *x;

struct coords {
    x: i32,
    y: i32
}

fn coords_origin() -> coords {
    return coords { x: 0, y: 1 };
}

fn print_coords(c: coords) {
    printInt(*(c.x));
    printInt(*(c.y));
    return;
}

export("fo_run") fn run() -> i32 {
    print_coords(coords_origin());
    return 5 + 2;
}