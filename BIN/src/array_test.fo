extern i8* malloc(i32 size);
//extern void free(i8* ptr);
//
//extern i32 puts(i8* str);
//extern void printInt(i32 x);
//
//extern void c_check_stack();
//
void consume_i8(i8 x) {
}
extern void make_i8(i8 x);
i32 run() {
    for(i32 y = 0; y < 512; y = y + 1;) {
        for(i32 x = 0; x < 512; x = x + 1;) {
            i8 ix = (i8) (x / 2);
            i8 iy = (i8) (y / 2);
//            make_i8(ix);
//            make_i8(iy);
            i8 r = ix ^ iy;
            i8 g = (~ix) & iy;
            i8 b = ix | (~iy);
            consume_i8(r);
            consume_i8(g);
            consume_i8(b);
        }
    }
    return 0;
}